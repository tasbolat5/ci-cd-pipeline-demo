{
  "name": "ci-cd-pipeline-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npx eslint src --ext .js || exit 1",
    "test": "npx jest",
    "build": "mkdir -p dist && cp src/index.html dist/index.html"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
  
    "jest": "^29.7.0"
  }
}
Add package.json
